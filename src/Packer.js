import rulesMap from rulesMap;
const packer={
 pack: function (state) {
     var iterator = rulesMap.values();
     var accum=""
     while (!iterator.done)
     state.get('Amount');

}

export default packer;